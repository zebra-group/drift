{
  "name": "@db-mirror/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "db-mirror": "./bin/db-mirror.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node bin/db-mirror.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@db-mirror/core": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
